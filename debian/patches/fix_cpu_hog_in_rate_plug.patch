Index: alsa-lib-1.0.15/src/pcm/pcm.c
===================================================================
--- alsa-lib-1.0.15.orig/src/pcm/pcm.c	2008-03-11 22:38:13.000000000 -0400
+++ alsa-lib-1.0.15/src/pcm/pcm.c	2008-03-11 22:38:27.000000000 -0400
@@ -5577,6 +5577,12 @@
 #endif
 {
 	assert(pcm && params);
+	/* Fix avail_min if it's below period size.  The period_size
+	 * defines the minimal wake-up timing accuracy, so it doesn't
+	 * make sense to set below that.
+	 */
+	if (val < pcm->period_size)
+		val = pcm->period_size;
 	params->avail_min = val;
 	return 0;
 }
